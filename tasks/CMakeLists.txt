# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(digiquartz_pressureTaskLib)
ADD_LIBRARY(${DIGIQUARTZ_PRESSURE_TASKLIB_NAME} SHARED 
    ${DIGIQUARTZ_PRESSURE_TASKLIB_SOURCES})

add_dependencies(${DIGIQUARTZ_PRESSURE_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${DIGIQUARTZ_PRESSURE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${DIGIQUARTZ_PRESSURE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${DIGIQUARTZ_PRESSURE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${DIGIQUARTZ_PRESSURE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${DIGIQUARTZ_PRESSURE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${DIGIQUARTZ_PRESSURE_TASKLIB_HEADERS}
    DESTINATION include/orocos/digiquartz_pressure)

